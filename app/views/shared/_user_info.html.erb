<%= link_to gravatar_for(current_user, size: Settings.user.size_img), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("user.view_profile"), current_user %></span>
<span><%= pluralize(current_user.microposts.count, t("microposts.post_text")) %></span>
