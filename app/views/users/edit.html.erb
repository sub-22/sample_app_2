<% provide :title, t("users.edit.subtitle_edit") %>
<% provide :button_text, t("users.edit.save_text") %>
<h1><%= t("users.edit.subtitle_edit") %></h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render "form" %>
    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <%= link_to t("users.edit.avatar_change"), Settings.user.edit.avatar_path, target: "_blank" %>
    </div>
  </div>
</div>
